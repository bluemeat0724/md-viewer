# Change Log

## 0.6.0（2026-08-20）

UI 全面重设计：冷静编辑感的中性冷灰 + 单一靛蓝强调色，GitHub 克隆观感升级为独立的视觉体系。

### 视觉与体系

- **设计令牌**：颜色 / 布局 / 圆角 / 字号 / z-index / 阴影全部集中于 `:root`，替换散落的魔法数与碎片字号（10.5 / 11.5 / 12.5px 等）。
- **双主题单源化**：主题色以 CSS `light-dark()` 定义一次（需 Chrome 123+ / Safari 17.5+ / Firefox 120+），深色令牌不再需要双块重复维护；打印固定浅色渲染，避免深色主题整页铺色。
- **图标系统**：内嵌 Tabler Icons v3（MIT）线性图标 sprite，替换全部 emoji / 文字符号（📚 🌗 🖨 ☰ ▶ 等）；brand 改为强调色浅底书本标记 + 标题。

### 新增

- **代码块一键复制**：悬停浮现复制按钮，成功后图标变对勾反馈 1.5s；`file://` 等非安全上下文自动降级 `execCommand`。
- **标题锚点**：悬停标题浮现 `#` 锚点，点击平滑滚动；事件委托拦截默认跳转，不污染 URL hash。
- **窄屏本页目录**：<1280px 时右侧大纲不可见，工具栏新增目录按钮唤出弹层面板，内容随文档切换刷新。
- **搜索增强**：输入框一键清空；无命中的分组整组隐藏，不再残留空分组标题。

### 排版与可达性

- 正文 15.5px / 行高 1.75；h1-h6 完整字阶 + `text-wrap:balance`；表格改横向强调风格（去竖线、表头粗底线，长中文表头可换行）。
- 补齐任务列表复选框、`kbd` 键帽、`mark` 高亮、`details/summary` 折叠、脚注与深色模式图片描边样式；blockquote 修复为正文对比度。
- 全局 `:focus-visible` 焦点环（此前仅搜索框有焦点样式）；图标按钮补齐 `aria-label`；`prefers-reduced-motion` 下动画与平滑滚动全量降级。
- 交互元素统一 140ms 颜色过渡、文档切换 180ms 淡入；目录树缩进改由 CSS `calc()` + `--d` 变量计算。
- 工具栏魔数治理：右侧大纲定位、标题滚动吸附与滚动探测基准统一由 `--toolbar-h` 令牌 / 实测工具栏高度推导。

### 清理

- 移除 viewer.js 中未被调用的客户端渲染管线 `renderMarkdown`（查看器始终使用构建时快照）与 CSS 中无引用的 `.tbtn:disabled` / `.tbtn[hidden]` 规则。

## 0.5.1

### 修复

- **内部链接按所在文档目录解析**：`rewriteLinks` 新增 `fromPath` 参数，子文档中的相对链接（`../README.md`、`./sub/deep.md` 等）按 Markdown 标准语义以「所在文档目录 + 链接」归一解析，命中后重写为页内 `#doc-<slug>` 跳转；未命中时退回扫描根相对查表（兼容根相对写法），越界（`../..` 超出扫描根）或缺失目标原样保留。
  - 构建端（`lib/build.mjs`）与浏览器实时渲染端（`viewer.js` 的 `renderMarkdown`）均已接入。
  - 新增纯函数 `normalizeRelPath`（posix 风格路径归一，不依赖 `node:path`，Node/浏览器双端共用），并随 `MDV_CORE` 导出。

### 新增

- **桌面端侧边栏折叠**：≥1024px 下可通过工具栏 ☰、侧边栏头部 × 或快捷键 **⌘/Ctrl+B** 折叠整栏，折叠偏好经 `localStorage`（键 `mdv-sidebar`）持久化并在下次打开时恢复；按钮提示与 `aria-label` 随状态同步。
  - 折叠时 `#app` 网格改为单列，避免侧边栏 `display:none` 后正文落入 0 宽列被挤成竖条。
  - 移动端不受影响，沿用原有抽屉遮罩（`sidebar-open`）交互。

### 测试

- `core.test.mjs`：新增 `rewriteLinks` 文档相对解析 / 根相对兜底 / 越界保留用例，新增 `normalizeRelPath` 用例。
- `build.test.mjs`：更新子文档 `../README.md` 链接断言为重写命中；新增折叠样式、持久化键、快捷键与单列网格的产物断言。

### 其他

- README 增加 change log 入口。

## 0.5.0

- 首个公开版本：扫描目录构建自包含、离线可用的 `md-viewer.html`。
